# Research

`rich-cli-kit` is a direct sladge candidate. Its README says it is built for
rich-CLI-capable agents, includes a FastMCP server, and lists related LLM/MCP
projects such as `cheap-llm-mcp`, `MCPForge`, and `PhenoMCP`.

Canonical `rich-cli-kit` is dirty with unrelated package, governance, test, and
documentation changes. The repository AGENTS contract requires feature work in
`repos/rich-cli-kit-wtrees/<topic>/`, so badge work is isolated there.

# Research

## Repository State

- Canonical rich-cli-kit is clean on `pr-34` at `2a39b06`.
- Older prepared branch `docs/rich-cli-kit-sladge-current` diverged by `1 1`,
  so commit `d2f5e47` is stale evidence for the active local head.
- Repo validation is exposed through Taskfile targets: `fmt`, `lint`, `test`,
  `build`, and `ci`.

## Existing Badge State

The current README did not contain `sladge.net` or `AI Slop Inside` before this
refresh.
